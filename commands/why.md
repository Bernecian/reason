---
description: Show which Reason rules fired for the last prompt (or one you pass)
---

Run `node "${CLAUDE_PLUGIN_ROOT}/scripts/why.js" $ARGUMENTS` and summarize the output for the user. If arguments are empty, the script uses `.r/.last-prompt`.
